import { userService, assignmentService, submissionService, walletService, notificationService, adminService, syndicateService, storageService } from './dbProvider';

console.log(userService.getUser);
